<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carwash.mapper.StoreMapper">

    <!-- map-underscore-to-camel-case=true 이므로 컬럼 별칭 불필요 -->
    <select id="findAll" resultType="com.carwash.domain.Store">
        SELECT id, name, bay_count, approved FROM store ORDER BY id
    </select>

    <!-- 승인된 매장만 (require 6.1) -->
    <select id="findApproved" resultType="com.carwash.domain.Store">
        SELECT id, name, bay_count, approved FROM store WHERE approved = TRUE ORDER BY id
    </select>

    <select id="findById" resultType="com.carwash.domain.Store">
        SELECT id, name, bay_count, approved FROM store WHERE id = #{id}
    </select>
</mapper>
